// Importing the 'Recipe' and 'User' classes from their respective modules
import { Recipe } from './recipe';
import { User } from './user';


// Exporting the 'Recipe' and 'User' classes to make them available for other modules that import this module
export {Recipe, User};